# --- Google Cloud Agent Platform / ADK (rules: Gemini + Agent Platform only) ---
google-adk>=1.0.0
google-cloud-aiplatform[agent_engines,adk]>=1.101.0
google-genai>=0.3.0

# --- Official ClickHouse MCP server (rules: active runtime use via mcp-clickhouse) ---
mcp-clickhouse[chdb]>=0.1.0

# --- Web surface (satisfies web-platform + Design criteria) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
authlib>=1.7.0
itsdangerous>=2.2.0
google-auth>=2.0.0

# --- Ingestion: external PDF coverage + .eml producer/agent emails ---
pdfplumber>=0.11.0
beautifulsoup4>=4.12.0

# --- Runtime MCP client ---
mcp>=1.0.0

# --- Local dev / testing ---
pytest>=8.0.0
